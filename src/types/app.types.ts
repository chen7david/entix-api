export enum Environment {
  Development = "development",
  Production = "production",
  Test = "test",
}

export enum EnvFile {
  DevelopmentEnv = ".env",
  TestEnv = ".env.test",
}

export enum DbName {
  Postgres = "postgres",
  EntixApi = "enitx-api",
  EntixApiTest = "entix-api-test",
}
